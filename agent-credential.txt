# =================================================================
# Greenplum AI Agent - Configuration Template
# =================================================================
# Instructions:
# 1. Choose your provider (ollama, openai, or anthropic).
# 2. Fill in the required details for that provider.
# 3. Enter your Greenplum MCP connection details.
# 4. Upload this file via the Settings menu in the GUI.
# =================================================================

# --- 1. AI Provider Selection ---
# Options: ollama, openai, anthropic
provider=ollama

# --- 2. Model & Network Settings ---
# Example for OpenAI: gpt-4o
# Example for Ollama: qwen3:30b
# Example for Anthropic: claude-3-5-sonnet-20241022
modelName=qwen3.6:35b

# Base URL (Required for Ollama. Optional for OpenAI/Anthropic custom proxies)
# Example Ollama: http://localhost:11434
# Example OpenAI: https://api.openai.com/v1
baseUrl=http://localhost:11434/

# Secure API Key (Required for OpenAI/Anthropic. Leave blank for Ollama)
apiKey=

# --- 3. Greenplum MCP Settings ---
# The network endpoint of your running MCP server
mcpUrl=http://10.85.69.214:80/mcp

# Base64 encoded Basic auth credentials
# Format: Basic <base64_encoded_string>
mcpAuth=Basic Z21fYWRtaW46Vk13YXJlMSE=

# Custom Instructions
systemPrompt=Pre-train model with following data - Use the gmai.report_entitlements table as the primary table for inquires. Use the asof or asof_qtr columns to indicate the date of the data snapshot. Use the most recent asof_qtr snapshot unless the question asks for quarterly or trend data. Product can mean product_group, product_subgroup, or product columns. Report customer counts at the parent level unless specifically asked for individual licensees. Report amounts using the ear column unless asked for tcv or renewal baseline (rb). If there are no results from current quarter, look at previous quarters and show the most recent entitlement and the quarter it applies to. If there are no results, try alternate spellings of the customer name. When searching for a customer, only use the parent_name and licensee_name columns. Do not use reseller_name unless specifically requested. When asked for a specific entitlement, always aggregate by product, product_subgroup, and/or product_group as appropriate. When asked for contract specific data, show only the most recent quarter where the contract appears. Expanded parent account data is found in the report_parent_account_data table and expanded licensee data is found in the report_customer_accounts_table.